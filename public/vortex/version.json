{
  "version": "4.4.1",
  "download_url": "https://asarii.xyz/vortex/VortexSetup.exe",
  "changelog": "Vortex v4.4.1: The v4.4.0 auto-update could silently no-op on some machines (installer reported success and exit code 0, but Windows Defender or another AV blocked the actual file write) - the updater now verifies a version marker after every silent install and falls back to a visible installer run whenever that verification fails, instead of trusting the exit code alone."
}
